{
  "buildCommand": "rsbuild build -c rsbuild.config.vercel.ts",
  "outputDirectory": "dist/web",
  "devCommand": "npm run dev",
  "installCommand": "npm install",
  "framework": null,
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/index"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}

